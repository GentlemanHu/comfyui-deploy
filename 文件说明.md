# 项目概述

ComfyUI Deploy 是一个围绕 ComfyUI 工作流部署的组合项目，包含 ComfyUI 自定义节点、ComfyUI 内嵌 Web 插件、Next.js 控制台、PostgreSQL 数据库迁移、S3 兼容对象存储上传接口，以及面向 Modal/RunPod/Classic ComfyUI 机器的运行调度逻辑。

# 快速开始

## Docker 本地启动控制台

1. 复制环境变量文件：
   `cp .env.example .env`

2. 设置环境变量：
   - `JWT_SECRET` 使用 `openssl rand -hex 32` 生成。
   - `SPACES_SECRET` 使用强密码，作为 MinIO 管理密码和 S3 密钥。
   - `LOCAL_AUTH_PASSWORD` 使用强密码，作为 Web 控制台访问密码。
   - 默认使用本地管理员身份，不需要 Clerk。
   - 使用宝塔/Nginx 反代时，通过根目录 `.env` 设置公网域名变量。

3. 启动：
   `docker compose -f docker-compose.local.yml up`

默认使用 GitHub Actions 预构建的 Web 镜像 `ghcr.io/gentlemanhu/comfyui-deploy-web:latest`，服务器启动时只执行数据库迁移和 `next start`，不会在低内存机器上执行 `next build`。

低内存服务器如果曾经出现 `exited with code 137`，不要在服务器上构建 Next.js。先等待 GitHub Actions 的 `Build local web image` 完成，再执行 `docker compose -f docker-compose.local.yml pull web` 拉取预构建镜像。

4. 访问：
   - Web 控制台：`http://localhost:3000`
   - MinIO S3：`http://localhost:9000`
   - MinIO 控制台：`http://localhost:9001`
   - PostgreSQL：`localhost:5480`

## 宝塔/Nginx 反代

推荐使用两个域名：

- `deploy.example.com`：反代到 `http://127.0.0.1:3000`
- `s3.example.com`：反代到 `http://127.0.0.1:9000`

MinIO 管理后台可选使用第三个域名 `minio.example.com` 反代到 `http://127.0.0.1:9001`，建议加 IP 白名单或 Basic Auth。

根目录 `.env` 示例：

```
SPACES_PUBLIC_ENDPOINT=https://s3.example.com
SPACES_ENDPOINT_CDN=https://s3.example.com
SPACES_SECRET=replace-with-a-strong-minio-password
JWT_SECRET=replace-with-openssl-rand-hex-32
LOCAL_AUTH_USER_ID=local-admin
LOCAL_AUTH_USER_NAME=Local Admin
LOCAL_AUTH_PASSWORD=replace-with-a-strong-password
COMFYDEPLOY_WEB_IMAGE=ghcr.io/gentlemanhu/comfyui-deploy-web:latest
```

可从 `.env.example` 复制：

`cp .env.example .env`

`JWT_SECRET`、`SPACES_SECRET`、`LOCAL_AUTH_PASSWORD` 是必填项；未设置时 `docker compose` 会拒绝启动。

低内存机器可保留默认：

```
COMFYDEPLOY_WEB_MODE=prod
NODE_OPTIONS=--max-old-space-size=1024
NEXT_PRIVATE_BUILD_WORKER=1
MINIO_SCANNER_SPEED=slowest
```

`deploy.example.com` 的 Nginx 关键配置：

```
client_max_body_size 1024m;

location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

`s3.example.com` 的 Nginx 关键配置：

```
client_max_body_size 1024m;

location / {
    proxy_pass http://127.0.0.1:9000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;
    proxy_request_buffering off;
}
```

S3 预签名 URL 对 `Host` 敏感，`s3.example.com` 必须保留 `proxy_set_header Host $host;`。不要给 `s3.example.com` 加宝塔 Basic Auth，否则浏览器直传和 ComfyUI 上传会失败。

## Paperspace/多 ComfyUI 节点

推荐域名分离：

- `comfydeploy.example.com`：Web 控制台和 `/api`，反代到 `127.0.0.1:3000`。
- `comfy-a.example.com`、`comfy-b.example.com`：不同 Paperspace/ComfyUI 节点，分别反代到各自 ComfyUI，是否开启 Basic Auth 由节点自己决定。

ComfyUI 插件中的 Endpoint URL 应填写 `https://comfydeploy.example.com`，不要填写 ComfyUI 节点域名。插件登录会打开 `https://comfydeploy.example.com/auth/request/:request_id`，并直接轮询 `https://comfydeploy.example.com/api/auth-response/:request_id`，不会再请求 ComfyUI 节点域名，因此不会触发 ComfyUI 自身 Basic Auth 弹窗。

在 Web 控制台添加 Classic Machine 时，Endpoint 才填写 ComfyUI 节点域名，例如 `https://comfy-a.example.com`。如果该 ComfyUI 节点有 Basic Auth，在 `auth_token` 填 `username:password`。

ComfyUI 节点域名如果开启 Basic Auth，浏览器访问 ComfyUI 时仍需通过一次该节点自己的 Basic Auth。插件内部调用 `/comfyui-deploy/*` 时会使用 `X-ComfyDeploy-Authorization` 传递 ComfyDeploy API Key，不会覆盖浏览器发给 ComfyUI Nginx 的 Basic Auth。

## ComfyUI 插件安装

将本项目放入 ComfyUI 的 `custom_nodes/comfyui-deploy` 目录后启动 ComfyUI。插件会注册 Comfy Deploy 节点、前端面板以及 `/comfyui-deploy/*` 路由。

# 项目结构

- `comfy-nodes/`：ComfyUI 自定义输入/输出节点。
- `web-plugin/`：注入 ComfyUI 前端的管理界面。
- `custom_routes.py`：ComfyUI 侧运行、上传、状态、机器同步等 HTTP 路由。
- `web/`：Next.js 控制台、API 路由、数据库 schema 和迁移。
- `web/drizzle/`：Drizzle 数据库迁移文件。
- `web/aws/`：旧 LocalStack S3 初始化脚本和 CORS 配置。
- `builder/modal-builder/`：Modal 构建器模板。
- `docker-compose.local.yml`：本地 PostgreSQL、Neon wsproxy、MinIO S3 和 Web 控制台一键启动。

# 文件说明

- `web/src/db/db.ts`：数据库连接入口；支持 `POSTGRES_WS_PROXY` 指定容器内 Neon WebSocket 代理。
- `web/src/server/localAuth.ts`：本地管理员认证适配，替代 Clerk 服务端认证。
- `web/src/lib/localClientAuth.ts`：本地管理员客户端认证适配。
- `web/src/server/resource.ts`：S3 兼容对象存储的预签名上传、下载、删除封装。
- `web/src/server/createRun.ts`：创建运行记录，并按机器类型调用 Classic/RunPod/Modal/ComfyDeploy 机器。
- `web/src/lib/externalInputs.ts`：统一提取和注入 ComfyUI Deploy 外部输入节点，覆盖文本、数字、布尔、枚举、Seed、文件、音频、EXR、LoRA、Checkpoint 等类型。
- `web/src/server/machineAuth.ts`：Classic Machine 请求认证处理，支持 Basic Auth、Bearer token 和 endpoint 内嵌账号密码。
- `web/src/middleware.ts`：本地 Web Basic Auth 保护；`/api` 和 ComfyUI 插件授权页保持公开，避免插件登录流程触发浏览器 Basic Auth 弹窗。
- `web/src/routes/registerCreateRunRoute.ts`：`POST /api/run` 工作流运行接口。
- `web/src/routes/registerUploadRoute.ts`：`GET /api/upload-url` 输入文件预签名上传接口。
- `web/src/app/(app)/api/file-upload/route.ts`：ComfyUI 输出文件上传 URL 生成接口。
- `custom_routes.py`：ComfyUI 侧接收运行请求、执行队列、上传输出、回传状态；代理 `/comfyui-deploy/*` 请求时支持 `X-ComfyDeploy-Authorization`，避免和 ComfyUI 节点 Basic Auth 冲突。
- `docker-compose.local.yml`：本地依赖和 Web 服务编排，内置本地默认环境变量。
- `web/Dockerfile.local`：GitHub Actions 用于预构建 Web 生产镜像。
- `.github/workflows/docker-web.yml`：自动构建并推送 `ghcr.io/gentlemanhu/comfyui-deploy-web`。
- `.env.example`：Docker Compose 读取的根目录环境变量模板，用于公网域名和本地用户设置。
- `web/.env.local.docker.example`：Docker 本地启动环境变量模板。
- `web/.env.local.docker`：旧版兼容模板；当前推荐使用根目录 `.env`。

# 技术栈

- ComfyUI 自定义节点：Python、aiohttp、Pydantic。
- Web 控制台：Next.js 14、React 18、Hono、本地认证、Tailwind、shadcn/ui。
- 数据库：PostgreSQL、Drizzle ORM、Neon serverless driver、本地 wsproxy。
- 对象存储：AWS SDK S3 兼容接口，默认接 MinIO，也可接 Cloudflare R2 或其他 S3 兼容服务。
- 容器：Docker Compose、Bun 运行时。

# 环境要求

- Docker 与 Docker Compose。
- 本地 Web 控制台需要可用端口：`3000`、`9000`、`9001`、`5480`、`5481`。
- 默认不需要 Clerk；本地管理员身份由 `LOCAL_AUTH_USER_ID` 和 `LOCAL_AUTH_USER_NAME` 控制。
- 运行真实 ComfyUI 工作流需要单独的 ComfyUI 实例和模型文件；本 compose 不包含 GPU ComfyUI 镜像。

# 注意事项

- 当前默认本地对象存储为 MinIO S3；如切换 Cloudflare R2，需要修改 `SPACES_ENDPOINT`、`SPACES_PUBLIC_ENDPOINT`、`SPACES_ENDPOINT_CDN`、`SPACES_REGION`、`SPACES_KEY`、`SPACES_SECRET`。
- Classic Machine 支持带 Basic Auth 的 ComfyUI endpoint。在添加 Custom Machine 时，`auth_token` 可填写 `username:password`，也可填写完整 `Basic base64` 或 `Bearer token`。如果 endpoint 写成 `https://user:pass@host`，服务端运行请求会自动转成 Basic Auth。
- 外部输入节点会在 Web 控制台、分享页和 `/api/run` 中统一解析。`External Enum` 的 `options` 支持 JSON 数组、换行列表或逗号分隔；数字和布尔会按节点类型转换后再写回 ComfyUI workflow API。
- ComfyUI 插件授权使用 `/auth/request/:request_id` 和 `/api/auth-response/:request_id`，插件会直接轮询 Web 控制台域名；部署、机器同步、模型同步等插件代理请求会把 ComfyDeploy API Key 放在 `X-ComfyDeploy-Authorization`，避免 ComfyUI 自身 Basic Auth 反复弹窗。
- 当前本地 compose 版本已使用本地管理员认证；公网暴露时必须放在 HTTPS Nginx 后面，并设置 `LOCAL_AUTH_PASSWORD` 或在宝塔/Nginx 加 Basic Auth/IP 白名单，同时限制 MinIO Console 访问。
- Web 控制台、文档页和分享页都受 `LOCAL_AUTH_PASSWORD` 保护；`/api` 保持公开入口，由 API Key/JWT 或内部回调自行鉴权。
- rclone/WebDAV 不能直接替代当前代码里的 S3 预签名 PUT 接口，除非通过 S3 兼容网关暴露。
- AI 生成和整理的配置需要在实际机器上测试验证。
